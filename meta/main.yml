---
dependencies: []

galaxy_info:
  role_name: ludus_splunk_uf_windows
  author: archb1sh0p
  description: An Ansible Role that installs the splunk Universal Forwarder agent on Windows and configures windows to send certain logs
  company: idk
  license: GPLv3
  min_ansible_version: "2.10"
  platforms:
    # Modify the platforms here as required
    # This example includes a lot, see https://github.com/ansible/ansible-lint/blob/main/src/ansiblelint/schemas/meta.json for all possibilities
    - name: Windows 
      versions:
        - "all"
  galaxy_tags:
    - splunk